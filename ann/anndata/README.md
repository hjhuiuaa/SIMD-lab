# 数据目录

将课程提供的 DEEP100K 子集二进制文件放在本目录（Windows）或服务器 `/anndata/`（Linux），例如：

- `DEEP100K.query.fbin`
- `DEEP100K.base.100k.fbin`
- `DEEP100K.gt.query.100k.top100.bin`

`*.fbin` 已由仓库 `.gitignore` 排除，不会上传到 Git。
